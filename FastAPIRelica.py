import BuildInitialOBJ
import CheckSku

def getThroughput():
        

        #Take Raw As400 data and roughly structure it         
        FinalResponse = BuildInitialOBJ.InitialObject()
        # print(FinalResponse)
        # #initialise variables below
        # TotalPackThroughputAllSteps = 0
        # ReturnOBJ = []
        # #Check Sku Against Know Lists to add product names to response object
        # newObject = CheckSku.AddProductNameToObject(FinalResponse)
        # #Check Object created above and add Step Names to response object
        # finalObject = CheckSku.AddStepNameToObject(newObject)
        
getThroughput()
